import { PageMarginDirective } from './page-margin.directive';

describe('PageMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new PageMarginDirective();
    expect(directive).toBeTruthy();
  });
});
